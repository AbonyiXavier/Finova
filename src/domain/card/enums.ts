export enum CardType {
  VISA = 'visa',
  MASTER = 'master',
}

export enum CardStatus {
  PENDING = 'pending',
  ACTIVATED = 'activate',
  DEACTIVATED = 'deactivate',
  EXPIRED = 'expired',
}

export enum SpendingLimitInterval {
  DAILY = 'daily',
  WEEKLY = 'weekly',
  MONTHLY = 'monthly',
}
